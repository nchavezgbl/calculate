---
applications:
  - name: calculate
    path: build/libs/calculate.jar
    routes:
      - route: calculate-neidi-keith.apps.evans.pal.pivotal.io
    env:
      WELCOME_MESSAGE: Hello from the review environment
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ } }'
